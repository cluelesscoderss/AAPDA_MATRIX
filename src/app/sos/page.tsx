
import VictimApp from "@/components/VictimApp";

export const metadata = {
          title: "SOS | Aapda Matrix Victim App",
          description: "Report an emergency immediately to rescue teams.",
};

export default function SOSPage() {
          return <VictimApp />;
}
